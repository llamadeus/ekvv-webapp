/**
 * The following constants allow more granularity over payloads created by actions creators.
 */
export const PAYLOAD = Symbol('PAYLOAD');
export const META = Symbol('META');
export const ERROR = Symbol('ERROR');
